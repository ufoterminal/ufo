// Where the API lives. Empty means "the same origin that served this page", which is the case when the
// Node app serves these files itself. Set it to your indexer's URL when the frontend is hosted apart
// from it, for example on Vercel or Cloudflare Pages:
//   window.TALONS_API = "https://your-indexer.example.com";
window.TALONS_API = "";
