// Address of the Worker that reads the launchpads. Empty means "same domain", which is the case when
// Pages and the Worker sit behind one hostname via a route. Otherwise put the workers.dev URL here.
window.TALONS_API = "";
